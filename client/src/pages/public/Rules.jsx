/**
 * Rules Page (Public)
 * 
 * Wrapper around the existing RulesPage component.
 * Accessible at route `/reglas`.
 */
import { RulesPage } from '../../components/RulesPage';

export function Rules() {
    return <RulesPage />;
}
