{
  "name": "impostor-game",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "start": "npm run start --workspace=@impostor/server",
    "test": "npm run test --workspace=@impostor/server"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
