{
  "name": "impostor-game",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development node server.js",
    "test": "jest --runInBand",
    "build": "cd client && npm install && npm run build",
    "postinstall": "npm run build",
    "all:dev": "concurrently -k -n SERVER,CLIENT -c blue,green \"npm run dev\" \"cd client && npm run dev\"",
    "dev:server+buildwatch": "concurrently -k -n SERVER,BUILD -c blue,magenta \"PORT=4000 npm run dev\" \"cd client && npm run build:watch\"",
    "all:build": "cd client && npm install && npm run build && cd .. && NODE_ENV=production node server.js",
    "prepare": "husky"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^12.6.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "husky": "^9.1.7",
    "imagemin": "^9.0.1",
    "imagemin-pngquant": "^10.0.0",
    "jest": "^29.7.0",
    "puppeteer": "^24.23.0",
    "sharp": "^0.34.4"
  }
}
